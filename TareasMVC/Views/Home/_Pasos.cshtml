<div class="editor-seccion-head">
    <label class="editor-label"><i class="bi bi-check2-square"></i> Pasos de la tarea</label>
    <button class="btn btn-secondary btn-sm" onclick="manejarClickAgregarPaso()">
        <i class="bi bi-plus-lg"></i>
    </button>
</div>

<div id="reordenable-pasos" data-bind="foreach: pasos">
    <div class="mt-2">
        <div class="row">
            <div class="col-1">
                <input type="checkbox" class="form-check-input" name="chbPaso"
                       data-bind="checked: realizado, 
                                   click: manejarClickCheckboxPaso,
                                   attr:{'data-id': id}" />
            </div>
            <div class="col-11">
                <span style="cursor: pointer" 
                data-bind="text: descripcion, hidden: modoEdicion,
                click: manejarClickDescripcionPaso,
                css: {'text-decoration-line-through': realizado}"></span>

                <textarea name="txtPasoDescripcion" class="form-control"
                          placeholder="Agregue una descripción"
                          data-bind="{value: descripcion, visible: modoEdicion}"></textarea>

                <div class="mt-2 d-flex gap-2" data-bind="{visible: modoEdicion}">
                    <button class="btn btn-primary btn-sm" data-bind="{click: manejarClickSalvarPaso}">
                        Salvar
                    </button>
                    <button class="btn btn-secondary btn-sm" data-bind="{click: manejarClickCancelarPaso}">
                        Cancelar
                    </button>
                    <button class="btn btn-danger btn-sm" data-bind="{hidden: esNuevo,
                            click: manejarClickBorrarPaso}">
                        Borrar
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>